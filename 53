 /*  53. Faça um programa para ler as dimens˜ões de um terreno (comprimento c e largura l),
 bem como o preço do metro de tela p. Imprima o custo para cercar este mesmo terreno
 com tela.  */



#include <stdio.h>
#include <stdlib.h>

int main()
{
    float comprimento, largura, preco_metro;
    float valor_total_tela, perimetro;

    printf("Digite o comprimento do terreno: \n");
    scanf("%f", &comprimento);

    printf("Digite a largura do terreno: \n");
    scanf("%f", &largura);

    printf("Digite o preco do metro da tela: \n");
    scanf("%f", &preco_metro);

    perimetro = 2*comprimento + 2*largura;

    valor_total_tela = perimetro * preco_metro;

    printf("O custo para cercar este terreno com tela vale: %.2f \n", valor_total_tela);

    system("pause");
    return 0;
}
